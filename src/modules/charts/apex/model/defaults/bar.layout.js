import { CARTESIAN_LAYOUT } from "./cartesian.layout";

export const BAR_CHART_LAYOUT = {
  ...CARTESIAN_LAYOUT,
  // bar
  horizontal: false,
  stacked: false,
  stacked100: false,
  barRadius: 1,
};
